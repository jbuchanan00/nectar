import type { PoolClient } from "pg";
import type { InstagramPost } from "../../../baseTypes";


export async function addInstagramPost(db: PoolClient, payload: InstagramPost, imageId: string){
    
}